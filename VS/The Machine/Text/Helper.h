#pragma once
#include <vector>
#include <string>

namespace helper {
	const std::vector<std::string> messages = {"Don't let the Anti-Viruses see you! They will chase you!","Dont listen to the OS. He always lies!"};
}